services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=sqlite:///./prosciutto.db
    # restart: always
